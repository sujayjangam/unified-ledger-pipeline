"""Throwaway file to deliberately fail the ruff lint check (unused import) for issue #34
verification: confirming a PR with a failing check cannot be merged into main. Deleted
before this PR is closed.
"""
import os
