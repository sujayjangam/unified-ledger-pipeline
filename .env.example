# Copy this file to .env and fill in real values. .env is gitignored - never commit it.

# --- Database ---
# Neon Postgres connection string. Use the DIRECT (unpooled) endpoint: SQLAlchemy runs its own
# connection pool, so Neon's PgBouncer-fronted endpoint would just stack a second pooler
# underneath it. Note the "+psycopg" driver suffix - plain "postgresql://" defaults to psycopg2,
# which is not installed.
DATABASE_URL=postgresql+psycopg://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# --- Telegram ---
TELEGRAM_BOT_TOKEN=123456789:AAaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

# JSON object mapping Telegram user ID (as a string) -> display name. This is the bot's allowlist;
# anyone not listed here is rejected by is_authorized().
ALLOWED_TG_IDS={"123456789": "user1name", "987654321": "user2name"}

# JSON object mapping a person's name -> list of their payment accounts/cards. The FIRST entry in
# each list is that person's default account. Used to reverse-look-up account_owner from whatever
# payment method was extracted from a voice note.
ACCOUNT_OWNERS={"user1name": ["pay_method_1", "pay_method_2"], "user2name": ["pay_method_3"]}

# Optional: feeds the extraction prompt's list of valid payment methods.
ALLOWED_ACCOUNTS=["pay_method_1", "pay_method_2", "pay_method_3", "pay_method_4"]

# --- OpenAI (Whisper transcription + GPT extraction) ---
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# --- Deployment ---
# Optional: only used by bot_webhook.py to register the Telegram webhook on startup.
# Leave unset for local development (bot_polling.py doesn't need it).
WEBHOOK_URL=https://your-service-xxxxx.a.run.app
