name: CI test before any PR merge to main

on: 
  pull_request: # left blank to apply to ALL PRs
  push:
      branches: [main]

jobs:
  check-PR-before-merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: test commit with git checkout
        run: ls -la